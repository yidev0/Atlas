tell application "System Events"    set returnName to {0, 0}        tell process "Dock" to tell list 1 to set dockItems to a reference to (every UI element whose role is "AXDockItem")        repeat with dockitem in dockItems        if name of dockitem = "Atlas" then            set returnPosition to position of dockitem            exit repeat        end if    end repeat        return returnPositionend tell